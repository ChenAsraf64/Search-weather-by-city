import './App.css';
import Weather from './Weather.js';

// Defining the main component of the application using the 'App' function
function App() {
  // Returning a JSX expression that represents the HTML structure of the application
  return (
    <div className="App">
      <Weather />
    </div>
  );
}

export default App;
